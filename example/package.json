{
  "name": "rust-nodejs-bundler-example",
  "version": "0.0.0-semantic-release",
  "private": true,
  "scripts": {
    "build": "cd web && esbuild ./src/index.ts --outfile=./dist/index.js --bundle --sourcemap --target=chrome94",
    "build:prod": "npm run build --minify"
  },
  "devDependencies": {
    "@types/node": "22.15.32",
    "esbuild": "0.25.5",
    "typescript": "5.8.3"
  }
}
