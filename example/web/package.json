{
  "name": "rust-nodejs-bundler-example",
  "version": "1.0.0",
  "author": "SpiralP <spiral.palmer@gmail.com>",
  "license": "MIT",
  "scripts": {
    "build": "esbuild src/index.ts --outfile=dist/index.js --bundle --sourcemap --target=chrome94",
    "build:prod": "yarn build --minify"
  },
  "devDependencies": {
    "@types/node": "^16.11.11",
    "esbuild": "^0.14.1",
    "typescript": "^4.5.2"
  }
}
